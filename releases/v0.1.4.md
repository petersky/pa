# v0.1.4 — 0.1.4

**Release track:** release  
**Date:** 2026-07-07

## Summary

Patch release focused on stability: fixes a crash on the Work page and a crash when running `pa update`, plus a lockfile sync so dependencies match the published package version.

## Highlights

- Fix Work page crash by passing Item objects to the items partial
- Fix `pa update` crash when comparing release versions
- Sync `uv.lock` with v0.1.3 package version

## Changed

- Sync `uv.lock` with v0.1.3 package version

## Fixed

- Work page crash: pass Item objects to items partial
- `pa update` crash when comparing release versions

## Upgrade

```bash
pa update
# or fresh install:
curl -fsSL https://raw.githubusercontent.com/petersky/pa/main/scripts/install-remote.sh | bash
```

## Changelog

- Fix Work page crash: pass Item objects to items partial. (db9afe5)
- Fix pa update crash when comparing release versions. (3305c3e)
